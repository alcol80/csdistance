============
Installation
============

At the command line::

    $ easy_install csdistance

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv csdistance
    $ pip install csdistance
